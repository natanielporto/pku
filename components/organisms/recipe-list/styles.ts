import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 8,
  },
  backButton: {
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
    marginLeft: 16,
    marginBottom: 8,
  },
  backText: {
    marginLeft: 8,
  },
  scrollView: {
    // ScrollView container
  },
  itemContainer: {
    paddingVertical: 8,
  },
});
